/* eslint-disable no-unused-vars */
// createItem.js
export function createItem(name, description) {
  // code to perform create item logic
  return { id: 1, name: name, description: description };
}
