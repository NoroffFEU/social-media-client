import {remove} from "./storage/remove.js";


// The logout function clears the token from browser storage.
describe("My key"); {
  it ("It clears the token from browser storage")
    expect(key).not.toBeTruthy(remove);

}
  