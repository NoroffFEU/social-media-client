import {save} from "./storage/save.js";


// The login function fetches and stores a token in browser storage.
describe("My token"); {
  it ("It fetches and stores a token in browser storage"); 
    expect(token).toEqual(secrets);

}
  

