name: Deploy Master

on:
  push:
    branches: ["master"]
  workflow_dispatch:

	@@ -15,13 +11,11 @@ permissions:
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
	@@ -30,7 +24,7 @@ jobs:
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install Dependencies
        run: npm i
      - name: Build SASS
        run: npm run build
	@@ -39,8 +33,7 @@ jobs:
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: "."
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1