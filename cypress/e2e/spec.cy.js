import { locatorObj } from "../pageLocators/locatorObj";

// describe("validates user inputs correctly based on API restrictions", () => {
//   it("User can login with valid inputs baded on API restrictions", () => {
//     cy.visit("http://127.0.0.1:5500/")
//   })
// } )
